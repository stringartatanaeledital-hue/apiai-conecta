# Copie este arquivo para ".env.local" e preencha com os valores reais.
# O NEXT_PUBLIC_CONVEX_URL é gerado automaticamente na primeira vez que
# você roda "npx convex dev" (veja o passo 2 do README-INSTALACAO.md).

NEXT_PUBLIC_CONVEX_URL=
